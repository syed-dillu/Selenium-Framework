pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                git url: 'https://github.com/syed-dillu/Selenium-Framework.git', branch: 'main'
                sh 'echo "Cloning the project" '
            }
        }
        stage('Install Dependencies') {
            steps {
                sh 'pip install -r requirements.txt' // Install Python dependencies
                sh 'echo "Installing requirements.txt.." '

            }
        }
        stage('Run Tests') {
            steps {
                sh 'pytest --alluredir=report/allure_results' // Run your tests
                sh 'echo "Running the Test Cases.."'
            }
        }
        stage('Generate Allure Report') {
            steps {
                sh 'allure serve report/allure_results' // Generate and serve Allure report
                sh 'echo "Generate the Report.."'
            }
        }
    }
    post {
        always {
            archiveArtifacts artifacts: 'report/allure_results/*', fingerprint: true // Archive test results
        }
    }
}
